# WordPress.org assets

`blueprints/blueprint.json` is the canonical source for the bbPress Plugin
Directory preview. It is copied separately to Plugin SVN as
`assets/blueprints/blueprint.json` during a stable release.

The `.wordpress-org` directory is development metadata and must not be included
in the bbPress plugin ZIP. Test the committer-only preview from the Plugin
Directory Advanced page before making it public.
